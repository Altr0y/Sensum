# Postman collection

Import `<collectionName>.postman_collection.json` into Postman.

Import `Sensum Local.postman_environment.example.json`, duplicate it, and fill in local values.

Do not commit real tokens, passwords or local environment files.